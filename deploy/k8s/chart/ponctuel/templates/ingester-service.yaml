apiVersion: v1
kind: Service
metadata:
  name: {{ include "ponctuel.fullname" . }}-ingester
  labels:
    {{- include "ponctuel.labels" . | nindent 4 }}
    app.kubernetes.io/component: ingester
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8081
      targetPort: http
  selector:
    {{- include "ponctuel.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ingester
