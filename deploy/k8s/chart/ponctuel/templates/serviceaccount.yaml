{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ponctuel.serviceAccountName" . }}
  labels:
    {{- include "ponctuel.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
